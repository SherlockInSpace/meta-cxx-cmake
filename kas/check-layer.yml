# Same build as qemuarm64.yml but with this layer left out of bblayers.conf,
# so yocto-check-layer can add it itself and test it against a clean base.
header:
  version: 23
  includes:
    - kas/qemuarm64.yml

repos:
  meta-cxx-cmake:
    layers:
      .: disabled
